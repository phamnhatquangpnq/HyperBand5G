<Application x:Class="WifiBandLockPro.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:WifiBandLockPro.App.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <converters:BoolToToggleTextConverter x:Key="BoolToToggleTextConverter"/>
            <converters:BoolToToggleColorConverter x:Key="BoolToToggleColorConverter"/>
            <converters:BoolToVisConverter x:Key="BoolToVisConverter"/>
            <converters:InverseBoolToVisConverter x:Key="InverseBoolToVisConverter"/>
            <converters:TabIndexToVisConverter x:Key="TabIndexToVisConverter"/>

            <!-- HyperBand 5G Design Palette (DynamicResource enabled for real-time theme switching) -->
            <SolidColorBrush x:Key="KillerDarkBrush" Color="#0A0D14"/>
            <SolidColorBrush x:Key="KillerPanelBrush" Color="#121722"/>
            <SolidColorBrush x:Key="KillerBorderBrush" Color="#1F293D"/>
            <SolidColorBrush x:Key="KillerAccentBrush" Color="#00D2FF"/>
            <SolidColorBrush x:Key="KillerGreenBrush" Color="#00F280"/>
            <SolidColorBrush x:Key="KillerTextBrush" Color="#E2E8F0"/>
            <SolidColorBrush x:Key="KillerMutedBrush" Color="#64748B"/>

            <!-- Sleek Button Style -->
            <Style x:Key="KillerButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource KillerAccentBrush}"/>
                <Setter Property="Foreground" Value="#0A0D14"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Small Secondary Button Style -->
            <Style x:Key="KillerSmallButtonStyle" TargetType="Button" BasedOn="{StaticResource KillerButtonStyle}">
                <Setter Property="Background" Value="#1A2333"/>
                <Setter Property="Foreground" Value="{DynamicResource KillerTextBrush}"/>
            </Style>

            <!-- Toggle Button Style -->
            <Style x:Key="KillerToggleButtonStyle" TargetType="Button" BasedOn="{StaticResource KillerButtonStyle}">
                <Setter Property="Foreground" Value="#0A0D14"/>
                <Setter Property="FontWeight" Value="ExtraBold"/>
            </Style>

            <!-- DataGrid Style -->
            <Style x:Key="KillerDataGridStyle" TargetType="DataGrid">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="{DynamicResource KillerTextBrush}"/>
                <Setter Property="RowBackground" Value="Transparent"/>
                <Setter Property="AlternatingRowBackground" Value="#0D121C"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource KillerBorderBrush}"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="ColumnHeaderStyle">
                    <Setter.Value>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource KillerMutedBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource KillerBorderBrush}"/>
                        </Style>
                    </Setter.Value>
                </Setter>
                <Setter Property="RowStyle">
                    <Setter.Value>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Padding" Value="0,6"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource KillerTextBrush}"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1A2333"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Setter.Value>
                </Setter>
                <Setter Property="CellStyle">
                    <Setter.Value>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{DynamicResource KillerTextBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Glowing Progress Bar Style -->
            <Style x:Key="KillerProgressBarStyle" TargetType="ProgressBar">
                <Setter Property="Background" Value="#1A2333"/>
                <Setter Property="Foreground" Value="{DynamicResource KillerAccentBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <Grid>
                                <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                                <Border x:Name="PART_Track" CornerRadius="3">
                                    <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="3" HorizontalAlignment="Left"/>
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Segmented Wi-Fi Signal Gauge Bar Style (Multi-chunk LED bar effect) -->
            <Style x:Key="SegmentedSignalBarStyle" TargetType="ProgressBar" BasedOn="{StaticResource KillerProgressBarStyle}">
                <Setter Property="Foreground" Value="{DynamicResource KillerGreenBrush}"/>
                <Setter Property="OpacityMask">
                    <Setter.Value>
                        <VisualBrush>
                            <VisualBrush.Visual>
                                <UniformGrid Columns="6" Width="60" Height="12">
                                    <Border Background="Black" Margin="0,0,2,0" CornerRadius="1"/>
                                    <Border Background="Black" Margin="0,0,2,0" CornerRadius="1"/>
                                    <Border Background="Black" Margin="0,0,2,0" CornerRadius="1"/>
                                    <Border Background="Black" Margin="0,0,2,0" CornerRadius="1"/>
                                    <Border Background="Black" Margin="0,0,2,0" CornerRadius="1"/>
                                    <Border Background="Black" CornerRadius="1"/>
                                </UniformGrid>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
